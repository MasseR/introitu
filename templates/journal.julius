$(function() {
    $("#itemForm").submit(function() {
        var url = $(this).attr('action');
        $.post(url,
               $(this).serialize(),
               function(data) {
                   $(data.item).prependTo('#results');
               });
        return false;
    });
});
